<header class="bg-gradient-to-r from-indigo-600 to-purple-600 text-white p-8 shadow-xl">
  <nav class="container mx-auto flex justify-between items-center">
    <ul class="flex space-x-6 items-center">
      <li>
        <a
          href="/"
          class="<%= currentPage === 'index' ? 'bg-gradient-to-r from-indigo-500 to-purple-500 font-semibold' : 'hover:bg-gradient-to-r from-indigo-400 to-purple-400' %> py-2 px-4 rounded-lg transition duration-300 text-lg font-bold"
          >✈ EliteStay</a
        >
      </li>
      <%if (isLoggedIn) { %>
        <%if (user && user.userType==="guest") { %>
      <li>
        <a
          href="/homes"
          class="<%= currentPage === 'Home' ? 'bg-gradient-to-r from-indigo-500 to-purple-500 font-semibold' : 'hover:bg-gradient-to-r from-indigo-400 to-purple-400' %> py-2 px-4 rounded-lg transition duration-300"
          >🏠 Homes</a
        >
      </li>
      <li>
        <a
          href="/favourites"
          class="<%= currentPage === 'favourites' ? 'bg-gradient-to-r from-indigo-500 to-purple-500 font-semibold' : 'hover:bg-gradient-to-r from-indigo-400 to-purple-400' %> py-2 px-4 rounded-lg transition duration-300"
          >❤️ Favourites</a
        >
      </li>
      <li>
        <a
          href="/bookings"
          class="<%= currentPage === 'bookings' ? 'bg-gradient-to-r from-indigo-500 to-purple-500 font-semibold' : 'hover:bg-gradient-to-r from-indigo-400 to-purple-400' %> py-2 px-4 rounded-lg transition duration-300"
          >📅 Bookings</a
        >
      </li>
      <% }else{ %>
      <li>
        <a
          href="/host/host-home-list"
          class="<%= currentPage === 'host-homes' ? 'bg-gradient-to-r from-indigo-500 to-purple-500 font-semibold' : 'hover:bg-gradient-to-r from-indigo-400 to-purple-400' %> py-2 px-4 rounded-lg transition duration-300"
          >🏢 Host Homes</a
        >
      </li>
      <li>
        <a
          href="/host/add-home"
          class="<%= currentPage === 'addHome' ? 'bg-gradient-to-r from-indigo-500 to-purple-500 font-semibold' : 'hover:bg-gradient-to-r from-indigo-400 to-purple-400' %> py-2 px-4 rounded-lg transition duration-300"
          >➕ Add Home</a
        >
      </li>
      <% } %>
      <% } %>
    </ul>
    <ul class="flex space-x-4 items-center">
      <%if (!isLoggedIn) { %>
      <li>
        <a
          href="/signup"
          class="hover:bg-gradient-to-r from-indigo-400 to-purple-400 py-2 px-5 rounded-lg transition duration-300 border border-white"
          >Sign Up</a>
      </li>
      <li>
        <a
          href="/login"
          class="bg-gradient-to-r from-indigo-500 to-purple-500 hover:bg-gradient-to-r from-indigo-600 to-purple-600 py-2 px-5 rounded-lg transition duration-300 font-semibold"
          >Login</a>
      </li>
      <% } else { %>
      <li>
        <form action="/logout" method="POST" style="display: inline;">
          <button
            type="submit"
            class="hover:bg-gradient-to-r from-indigo-400 to-purple-400 py-2 px-5 rounded-lg transition duration-300 bg-gradient-to-r from-indigo-500 to-purple-500 border-none cursor-pointer text-white font-semibold"
          >
            Logout
          </button>
        </form>
      </li>
      <% } %>
    </ul>
  </nav>
</header>